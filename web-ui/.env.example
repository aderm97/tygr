# TYGR Security Agent - Web UI Configuration

# Optional: Pre-configure default LLM settings
# Users can override these in the UI
NEXT_PUBLIC_DEFAULT_LLM_MODEL=openai/gpt-5
NEXT_PUBLIC_DEFAULT_LLM_API_BASE=

# Docker configuration
NEXT_PUBLIC_DOCKER_IMAGE=ghcr.io/aderm97/tygr-sandbox:latest

# Application settings
NEXT_PUBLIC_MAX_CONCURRENT_SCANS=5

# Development
NODE_ENV=development
